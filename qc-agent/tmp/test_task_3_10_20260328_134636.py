import asyncio\nfrom playwright.async_api import async_playwright\n\nasync def run_test():\n    async with async_playwright() as p:\n        browser = await p.chromium.launch(headless=False)\n        context = await browser.new_context()\n        page = await context.new_page()\n        # Navigate to login page\n        await page.goto(\"https://example.com/login\")\n        # Fill in username with mixed case (case‑insensitive test)\n        await page.fill(\"#username\", \"UsEr@Example.Com\")\n        # Fill in correct password\n        await page.fill(\"#password\", \"CorrectPassword\")\n        # Click the submit/login button\n        await page.click(\"#loginButton\")\n        # Wait for navigation to dashboard (example selector)\n        await page.wait_for_selector(\"#dashboard\", timeout=10000)\n        print(\"Login successful, dashboard loaded.\")\n        await context.close()\n        await browser.close()\n\nasyncio.run(run_test())